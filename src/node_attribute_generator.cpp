#include "node_attribute_generator.h"

void NodeAttributeGenerator::generateAttributes(std::ostream &a_OutputStream) {
    // TODO(thom): add this back.
    a_OutputStream << "Attribute generation is added soon." << "\n";
}
